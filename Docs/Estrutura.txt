Solução: Cad_Simples_Restfull
├── Connected Services/               <- Serviços conectados (opcional)
│    └── Banco de dados do SQL Server  <- Só se gerar EDMX ou scaffolding
│
├── Dependências/
│    ├── Analisadores/
│    ├── Estruturas/
│    └── Pacotes/
│
├── .github/
│   └── workflows/
│       └── ci-cd.yml
│
├── ApiUsuarios/                     <- Web API .NET 6 (Back-end)
│    ├── Controllers/                  <- Controllers REST (UsuariosController.cs)
│    ├── Data/                         <- DbContext e Configurações do EF Core
│    ├── Models/                       <- Models/Entities (Usuario.cs)
│    ├── Migrations/                   <- Migrations do Entity Framework
│    ├── Properties/
│    │    └── launchSettings.json
│    ├── appsettings.json              <- Conexão com SQL Server e configs da API
│    └── Program.cs                    <- Inicialização da API e Swagger
│
├── Docs/                             <- Documentação, estrutura em txt
│    ├── Estrutura.txt                 <- Toda estrutura em txt
│    └── README.md                     <- Passo a passo de instalação
│
├── Sql/                             <- Documentação, scripts SQL, diagramas
│    └── Procedures.sql               <- Script com todas as SPs
│
├── WinFormsUsuarios/                 <- Aplicação WinForms (Front-end MVC)
│    ├── Controllers/                  <- Controllers de comunicação com a API
│    ├── Models/                       <- Models para consumo da API
│    ├── Properties/
│    ├── Views/                        <- Telas (FormLogin, FormUsuarios, Modal)
│    └── Program.cs                    <- Inicialização da aplicação WinForms
│
├── .gitattributes
├── .gitignore
│
└── Cad_Simples_Restfull.sln         <- Arquivo da solução principal